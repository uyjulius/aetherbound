{
  "name": "aetherbound",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Aetherbound — a browser RPG rebuilt from a blank runtime",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node --test tests/*.test.js",
    "smoke": "node scripts/smoke.mjs",
    "check": "npm test && npm run build && npm run smoke"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.62.1"
  }
}
